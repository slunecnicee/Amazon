import Header from "../components/Header/Header";

const AllProducts = () => {
  //     const dispatch = useDispatch();
  //     const {products,categories,mostDemanded,offers,latest,allProducts}=useSelector((state)=>state.data)
  //     const {isSignedin}=useSelector((state)=>state.user)

  // useEffect(() => {

  //       const mergedProducts = [...products, ...mostDemanded, ...offers, ...latest];

  //    console.log(mergedProducts)
  //       dispatch(setAllProducts(mergedProducts));

  // },[]);

  return (
    <>
      <Header />
      protected Page Prolly will be cart
    </>
  );
};

export default AllProducts;
